# requirements.txt
PySide6>=6.4.0
qasync>=0.23.0
supabase>=1.0.0
# TODO: Thêm các thư viện sau nếu bạn sử dụng chúng cho livestream và các chức năng khác:
# (Ví dụ: nếu bạn dùng OpenCV và NumPy cho livestream)
# opencv-python>=4.5.0
# numpy>=1.20.0
# Pillow>=9.0.0 # Nếu dùng cho xử lý ảnh trong livestream
# sounddevice>=0.4.0 # Nếu dùng cho xử lý âm thanh cho livestream
# pyaudio # Một lựa chọn khác cho âm thanh